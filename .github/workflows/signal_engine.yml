name: SignalPro — Live Signal Engine

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  run-signals:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Repo checkout
        uses: actions/checkout@v4.2.2

      - name: Python setup
        uses: actions/setup-python@v5.4.0
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Dependencies install
        run: pip install requests pandas numpy --quiet

      - name: Signal engine run karo
        env:
          TWELVE_DATA_API_KEY: ${{ secrets.TWELVE_DATA_KEY }}
          TELEGRAM_BOT_TOKEN:  ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHANNEL:    ${{ secrets.TELEGRAM_CHANNEL }}
        run: python signal_engine_simple.py

      - name: Run summary
        if: always()
        run: echo "Scan complete at $(date -u)"
```

**"Commit changes"** click karo — warning bilkul band ho jaayegi! ✅

---

Aur ek important cheez — **"Signal engine run karo" step ka output** kya tha? Woh expand karke dekho — wahan likha hoga:
```
Checking XAU/USD...
  No signal — market unclear
